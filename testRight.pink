Program Pelos;
VARS
int: i,j,k, arreg[10];
float: val;

function int: fact (int: j, float: f)
VARS
    int: x;
{
    x = j + (f - j * 2 + j);
    if(j == 1){
        return(j);
    }else{
return(j * (j-1));
    }
}
main(){}