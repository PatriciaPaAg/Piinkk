Program Pelos;
VARS
int: i,j,k, arreg[10];
float: val;

function int: fact (int: a, float: b)
VARS
    int: x, f;
    float: y;
{
    x = j + (f - j * x * j);
    if(j == f){
        return(j);
    }else{
return(j * (j-k));
    }
}

function float: fibo (int: a, float: b, bool: arrBool[2])
VARS
    int: x, y;
    float: z, f;
{
    x = j + (f - j * i + j);
    if(j == z){
        return(j);
    }else{
return(j * (j-z));
    }
}

main(){}