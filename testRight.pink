Program Pelos;
VARS
int: x,y,z, arreg[10];
float: val;

function int: fact (int: a, int: b)
VARS
    int: c, d;
    int: e;
    bool: bo;
    int: sip, nop;
{
    x = b + c - d - 1 * e * a;
    bo = sip + c + d * e < nop * e + c + d ;
    write(sip + c + d, nop + sip);
    write('HOLA MUNDOTE ROSA', 'sigues ahi?');
    read(p);
}

<3 this is a comment

function float: fibo (float: f, int: g, bool: arrBool[2])
VARS
    int: h, i;
    int: j, k;
{
    f = g + h - i / 3.14 * j + k;
    if(x == y){
        return(j);
    }else{
        return(f * g - i);
    }
    <3 if(z){}
}

main(){
    while(x != y) do{
        val = z + x - y / 3.14 * x + z;
    }
    write('FIN');
}




