
Program Test;

VARS
    int: i, j, p, arreglo[10];
    float: valor;

function int fact(int: j)
VARS int i;
{
    i = j + (p - j * 2 + j);
    if(j == 1) then {
        return(j);
    }else {
        return(j * fact(j - 1))
    }
}

